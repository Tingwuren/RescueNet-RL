services:
  rescuenet:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        VITE_API_BASE: /api
    container_name: rescuenet-rl
    environment:
      FRONTEND_DIST: /app/frontend/dist
    ports:
      - "8000:8000"
    volumes:
      - ./artifacts:/app/artifacts
    restart: unless-stopped
